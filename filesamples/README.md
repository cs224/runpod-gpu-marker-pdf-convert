# filesamples

Committed sample input files for manual testing.

Subdirectories:

- `pdf/`: sample PDF files for [`pdfconvert-gpu`](/workspace/pdfconvert-gpu) and [`pdfconvert-cpu`](/workspace/pdfconvert-cpu)
- `office/`: sample `.docx` and `.pptx` files for [`officeconvert-cpu`](/workspace/officeconvert-cpu)
